export function createCards (num) {
    const containerCard = document.createElement("div");
    containerCard.className = `${num.themeCard}`;

    const datesHeader = document.createElement("div");
    datesHeader.className = "card__header card-red"
    const dates = document.createElement("div");
    dates.className = "card__header card-purple";
    const dates = document.createElement("div");
    dates.className = "card__header card-gray";
   
}